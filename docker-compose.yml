name: aap-loyalty-intelligence

services:
  web:
    build: ./web
    image: aap-loyalty-intelligence
    container_name: aap-loyalty-intelligence
    ports:
      - "8000:8000"
    env_file:
      - ./web/.env
    environment:
      - PORT=8000
    restart: unless-stopped
