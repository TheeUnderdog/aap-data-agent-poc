services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Override port to 8000 inside the container (matches gunicorn CMD)
      - PORT=8000
    restart: unless-stopped
