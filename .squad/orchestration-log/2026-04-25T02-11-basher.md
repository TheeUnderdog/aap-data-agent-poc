# Orchestration Log — Basher (2026-04-25T02:11 UTC)

## Spawn Manifest

| Field | Value |
|-------|-------|
| **Agent** | Basher (Backend Dev) |
| **Mode** | background |
| **Task** | Azure Static Web Apps setup |
| **Rationale** | User requested SWA deployment config. Basher owns backend/deployment. |

## Outcome

**Status:** ✅ SUCCESS

### Files Created (9 total)

1. `web/staticwebapp.config.json` — SWA routing, Entra ID auth, security headers, role-based access
2. `api/function_app.py` — Python v2 Azure Functions API (chat, user, health endpoints)
3. `api/requirements.txt` — Python dependencies (azure-functions, requests, openai, etc.)
4. `api/host.json` — Azure Functions runtime config
5. `api/local.settings.json` — Local dev settings template
6. `api/.funcignore` — Ignore patterns for Functions deployment
7. `.github/workflows/azure-static-web-apps.yml` — CI/CD pipeline (GitHub Actions)
8. `web/SETUP.md` — Complete SWA setup guide (portal + CLI creation, Entra ID registration, managed identity, auth flow)
9. `web/config.js` — Updated with SWA proxy clarification

### Technical Decisions

- **SWA + managed Azure Functions** for simplicity (vs Container Apps)
- **DefaultAzureCredential** for Fabric API auth (managed identity in prod, local CLI creds in dev)
- **Entra ID SSO** via `staticwebapp.config.json` with MSIT tenant v2.0 endpoint
- **Role-based routing** — all routes require `authenticated` role; 401 redirects to login
- **SSE streaming note:** Functions v2 Consumption accumulates events (true streaming requires Flex Consumption plan)
- **Local dev unchanged:** `web/server.py` still works with `useProxy: true`

### Key Implementation Details

**SWA Configuration:**
- Routes: `/api/*` → managed Functions, `/*` → static web app with SPA fallback
- Auth: MSIT tenant issuer, client ID/secret via app settings, OpenID Connect flow
- Security: CSP, X-Frame-Options, no-referrer, require-https

**Azure Functions (`api/function_app.py`):**
- `/api/chat` — SSE proxy to Fabric Data Agent (stream results)
- `/api/user` — Decodes `x-ms-client-principal` header from SWA
- `/api/health` — Liveness probe
- Assistant cache pattern replicated (note: per-instance in Functions, not persistent)

**CI/CD Pipeline (`azure-static-web-apps.yml`):**
- Builds `web/` (SPA) and `api/` (Functions separately)
- Deploy to SWA with managed Functions integration
- Trigger: push to main

### Prerequisites for Deployment

1. **Azure resources:** SWA instance, Functions runtime, managed identity
2. **Entra ID app registration:** Client ID, client secret, redirect URI
3. **GitHub secrets:** `AZURE_STATIC_WEB_APPS_API_TOKEN_*`
4. **Local dev:** `api/local.settings.json` with Azure CLI token, Fabric client ID/secret

### Next Steps for Linus (Frontend)

- Web app (`web/`) can now target `useProxy: true` (handled by SWA managed Functions)
- All Fabric API auth delegated to backend — frontend calls `/api/chat`, no direct Fabric access needed
- Mock integration still works via `web/server.py` for local dev

### Notes

- **Decision point:** First-time MSIT Entra ID app registration requires manual portal (App Registrations > New Registration). Subsequent deployments via script.
- **SWA managed Functions:** Automatic scaling, no container registry, integrated auth — ideal for POC.
- **Cost:** Pay-as-you-go (typical SWA POC: $0–15/month).

---

**Scribed by:** Copilot (Scribe)  
**Date:** 2026-04-25T02:11:00Z
